{
  "name": "@r1ogatix/common",
  "version": "1.0.4",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "author": "r1oga",
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && yarn run build && yarn publish --patch"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.1.2"
  }
}
